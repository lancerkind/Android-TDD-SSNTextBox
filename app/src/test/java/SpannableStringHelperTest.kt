package com.example.texteditbasicactivity

import android.graphics.Color
import android.text.SpannableStringBuilder
import mywidgets.SpannableStringHelper
import org.junit.Before
import org.junit.Ignore
import org.junit.Test

class SpannableStringHelperTest {
    lateinit var helper : SpannableStringHelper

    @Before
    fun before() {
    }

    @Ignore
    @Test
    fun getColor(){
        /*
        val string  = SpannableStringBuilder("h")

        helper = SpannableStringHelper(string)
        assertEquals(Color.BLACK, helper.getColor(0))

         */
    }
}